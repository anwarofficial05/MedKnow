version: '3.8'

services:
  web:
    build: .
    container_name: medknow-app
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=medknow-docker-secret-key-kmp-2026
      - PORT=5000
    volumes:
      - medknow-data:/app
    restart: unless-stopped

volumes:
  medknow-data:
